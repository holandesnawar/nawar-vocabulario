// DEPRECATED: La lógica de comunicación de altura con el iframe padre
// vive ahora en src/app/IframeHeightWrapper.tsx. Este archivo se conserva
// solo para evitar romper imports externos; puede eliminarse con
// seguridad si nadie lo referencia.
export function useIframeHeight(): void {
  // no-op
}
